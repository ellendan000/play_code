subprojects {
    buildscript {
        repositories {
            mavenCentral()
            maven { url 'https://repo.spring.io/release/' }
        }
    }

    group "top.bujiaban"

    repositories {
        mavenCentral()
        maven { url 'https://repo.spring.io/release/' }
    }

    configurations.all {
        resolutionStrategy.cacheChangingModulesFor 60, "minutes"
    }

}
